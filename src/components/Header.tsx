import Logo from './../../public/0755.gif'

const Header = () => {
    return (
        <div className="header">
            <img src={Logo} alt="logo"/>
        </div>
    );
};

export default Header;